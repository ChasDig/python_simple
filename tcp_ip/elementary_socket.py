import socket

# print(socket.gethostbyname_ex("www.google.com"))

# socket_info = socket.getaddrinfo("www.crappytaxidermy.com", 80)
# print(f"Info for UDP: {socket_info[1]}\nInfo for TCP: {socket_info[0]}")

# socket_info_tcp = socket.getaddrinfo("www.crappytaxidermy.com", 80, socket.AF_INET, socket.SOCK_STREAM)
# print(f"Info for TCP: {socket_info_tcp}")

# port_tcp = socket.getservbyname("http")
# print("Номер порта для сервиса 'http': ", port_tcp)
# name_tcp = socket.getservbyport(80)
# print("Имя сервиса для порта '80': ", name_tcp)
